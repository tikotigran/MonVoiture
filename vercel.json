{
  "version": 2,
  "framework": "nextjs",
  "buildCommand": "rm -rf node_modules && pnpm install --no-frozen-lockfile && next build",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "installCommand": "pnpm install --no-frozen-lockfile"
}
